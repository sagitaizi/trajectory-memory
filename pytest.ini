[pytest]
pythonpath = .
testpaths = tests
# nengo's bundled pytest11 plugin has a pre-pytest-9 hook signature that fails
# collection -- we don't use it (see the Thesis repo's pytest.ini).
addopts = -ra -p no:nengo
